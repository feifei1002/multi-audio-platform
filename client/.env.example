EXPO_PUBLIC_API_URL=http://YOUR_IP:8080

# Replace YOUR_IP with your actual local IP address, 
# which can be found by running `ipconfig` (Windows) or `ifconfig` (Mac/Linux) in the terminal.
# This allows the Expo app to communicate with the backend server running on your machine.